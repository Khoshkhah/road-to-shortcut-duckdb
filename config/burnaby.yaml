# Burnaby dataset configuration
# ==============================

# Input Configuration
input:
  district: "Burnaby"
  edges_file: "/home/kaveh/projects/osm-to-road/data/output/Burnaby/Burnaby_driving_simplified_edges_with_h3.csv"
  graph_file: "/home/kaveh/projects/osm-to-road/data/output/Burnaby/Burnaby_driving_edge_graph.csv"

# Output Configuration  
output:
  directory: "/home/kaveh/projects/road-to-shortcut-duckdb/output"
  shortcuts_file: "Burnaby_shortcuts"
  persist_dir: "/home/kaveh/projects/road-to-shortcut-duckdb/persist"

# Algorithm Configuration
algorithm:
  sp_method: "HYBRID"  # SCIPY, PURE, HYBRID
  hybrid_res: 10       # For HYBRID: PURE for res >= 10, SCIPY for res < 10
  partition_res: 7

# Parallel Execution
parallel:
  workers: 1
  workers_phase1: 1
  workers_phase4: 1

# DuckDB Configuration
duckdb:
  memory_limit: "12GB"
  threads: null        # null = auto-detect
  fresh_start: True   # If true, delete existing DB before running
